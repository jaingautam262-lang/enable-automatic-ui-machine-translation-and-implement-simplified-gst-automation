import { enTranslations } from './en';

// Catalan translation dictionary with English fallback
export const caTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Catalan-specific overrides here as needed
};

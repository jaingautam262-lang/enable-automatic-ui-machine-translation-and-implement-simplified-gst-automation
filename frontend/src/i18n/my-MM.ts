import { enTranslations } from './en';

// Burmese translation dictionary with English fallback
export const myTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Burmese-specific overrides here as needed
};

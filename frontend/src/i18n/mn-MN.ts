import { enTranslations } from './en';

// Mongolian translation dictionary with English fallback
export const mnTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Mongolian-specific overrides here as needed
};

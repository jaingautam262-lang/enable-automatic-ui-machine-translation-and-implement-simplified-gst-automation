import { enTranslations } from './en';

// Galician translation dictionary with English fallback
export const glTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Galician-specific overrides here as needed
};

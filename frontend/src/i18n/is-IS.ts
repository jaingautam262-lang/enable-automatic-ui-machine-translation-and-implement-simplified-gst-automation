import { enTranslations } from './en';

// Icelandic translation dictionary with English fallback
export const isTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Icelandic-specific overrides here as needed
};

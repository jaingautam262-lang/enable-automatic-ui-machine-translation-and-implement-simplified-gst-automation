// Chinese (Simplified) translation dictionary
// Minimal overrides - most translations will use machine translation fallback

export const zhTranslations: Record<string, any> = {
  // Only include intentional overrides here
  // Missing keys will trigger automatic translation
};

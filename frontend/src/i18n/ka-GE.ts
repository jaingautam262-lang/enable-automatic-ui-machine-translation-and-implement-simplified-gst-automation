import { enTranslations } from './en';

// Georgian translation dictionary with English fallback
export const kaTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Georgian-specific overrides here as needed
};

import { enTranslations } from "./en";

// Azerbaijani translation dictionary with English fallback
export const azTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Azerbaijani-specific overrides here as needed
};

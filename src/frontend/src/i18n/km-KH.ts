import { enTranslations } from "./en";

// Khmer translation dictionary with English fallback
export const kmTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Khmer-specific overrides here as needed
};

import { enTranslations } from "./en";

// Lao translation dictionary with English fallback
export const loTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Lao-specific overrides here as needed
};

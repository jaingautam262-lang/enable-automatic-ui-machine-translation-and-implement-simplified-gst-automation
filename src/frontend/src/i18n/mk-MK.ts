import { enTranslations } from "./en";

// Macedonian translation dictionary with English fallback
export const mkTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Macedonian-specific overrides here as needed
};

import { enTranslations } from "./en";

// Zulu translation dictionary with English fallback
export const zuTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Zulu-specific overrides here as needed
};

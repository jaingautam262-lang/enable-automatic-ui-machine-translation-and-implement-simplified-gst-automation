import { enTranslations } from "./en";

// Amharic translation dictionary with English fallback
export const amTranslations: Record<string, any> = {
  ...enTranslations,
  // Add Amharic-specific overrides here as needed
};
